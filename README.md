# Web Project Workspace

This workspace now keeps the React version of the project as the single active application.

## Active Project

The main application lives in:

```text
react-transport/
```

That folder contains the complete React, Vite, and React Router based version of the project.

## Current Structure

- `react-transport/`: active application source code
- `assets/`: shared image and PDF assets used by the project

## Notes

- The old standalone HTML/CSS pages were removed after the full React migration.
- If you want to run or build the project, use the commands inside `react-transport/`.
- Project-level documentation is available in [react-transport/README.md](C:\Users\MSI-NB\OneDrive\Masaüstü\web_projesi\react-transport\README.md).
